# Статические SVG

Шаблонные и прочие SVG, которые отдаются как файлы (не React-компоненты).

**Пример:** `file.svg` → `/icons/file.svg`

Интерактивные векторные иконки интерфейса — в `src/shared/ui/icon/` как `.tsx`.
